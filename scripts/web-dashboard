#!/bin/sh

project_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
cd "$project_root" || exit 1

export PYTHONPATH="$project_root${PYTHONPATH:+:$PYTHONPATH}"
exec "$project_root/.venv/bin/python" -m streamlit run app/dashboard/main.py "$@"
